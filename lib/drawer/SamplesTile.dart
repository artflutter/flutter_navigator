import 'package:flutter/material.dart';

class SamplesTile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListTile(
      title: Text(
        'SAMPLES',
        style: TextStyle(fontWeight: FontWeight.bold),
      ),
    );
  }
}
